# P1-07 (2026-05-09) — Dev-only dependencies.
#
# Install:
#     py -3.11 -m pip install -r requirements-dev.txt
#
# Keep separate from runtime requirements.txt to avoid bloating production
# install size and to keep lint/type-check tools out of the live bot env.

# --- Lint ------------------------------------------------------------------
ruff==0.6.9

# --- Type check -----------------------------------------------------------
mypy==1.13.0

# --- Test (already in requirements.txt but re-pin for dev clarity) --------
pytest==8.3.3
pytest-cov==5.0.0
pytest-asyncio==0.24.0

# --- Type stubs (3rd-party libs that ship without inline types) -----------
types-requests==2.32.0.20240914
types-cachetools==5.5.0.20240820

# --- P1-02 (2026-05-11) AI Advisor service ---------------------------------
# FastAPI + uvicorn for the out-of-process ai_advisor service. httpx is the
# bot-side HTTP client (already pulled in transitively by py-clob-client but
# pin here for the dev install to be self-sufficient).
fastapi==0.115.0
uvicorn[standard]==0.31.0
httpx==0.27.2
